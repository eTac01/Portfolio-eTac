✅ DAILY WORKFLOW — GITHUB PAGES UPDATE (FINAL)
1️⃣ Go to Your Project Folder
cd ~/Videos/Portfolio\ resume/new-portfolio-main

2️⃣ Make Sure You’re on the Correct Branch
git checkout main


(If it says “Already on 'main'” → good)

3️⃣ Check What Changed (Optional but Recommended)
git status

4️⃣ Stage ALL Changes
git add .


(Stages HTML, CSS, JS, images, assets — everything)

5️⃣ Commit With a Clear Message
git commit -m "Describe what you changed"


Examples:

git commit -m "Update CIA triad animation position"
git commit -m "Improve hero section animation"
git commit -m "Add new project cards"

6️⃣ Push to GitHub (This Updates the Live Site)
git push origin main

7️⃣ Wait & Refresh

Wait 1–2 minutes

Open:

https://etac01.github.io/Portfolio-eTac/


Hard refresh:

Ctrl + Shift + R

🔁 THAT’S IT — EVERY TIME

You will repeat only these commands:

git checkout main
git add .
git commit -m "Your message"
git push origin main
